// Drizzle schema for the clinic booking API.
//
// Tables are added per PLAN.md §4 as the app grows. Each domain module owns its
// tables; anything exported from this module is available to the Drizzle driver.

export {};
